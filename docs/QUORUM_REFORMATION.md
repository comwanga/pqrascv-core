# Quorum Reformation

Quorum reformation is a distinct, append-only governance event. It requires explicit cryptographic linkages to the previous quorum state to prevent accidental resets, history truncation, and hidden federation replacement.

See `crates/pqrascv-hardware/src/quorum_reformation.rs`.
