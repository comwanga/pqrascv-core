# Recovery Lineage

Explicitly models recovery ancestry via the `RecoveryLineage` struct (`lineage_root`, `parent_lineage`, `recovery_epoch`). This ensures deterministic historical reconstruction, forensic federation analysis, and migration lineage tracing.

See `crates/pqrascv-hardware/src/recovery_lineage.rs`.
