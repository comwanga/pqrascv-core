# Partition Healing

Healing requires explicit governance decisions (`PartitionHealingDecision`). Silent auto-merging is strictly forbidden. Conflicting histories are explicitly tracked.

See `crates/pqrascv-hardware/src/partition_healing.rs`.
