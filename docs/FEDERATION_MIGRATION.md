# Federation Migration

Federation migration preserves governance continuity and verifier lineage while transitioning between federation topologies. It relies on a cryptographic root binding the old state to the new state.

See `crates/pqrascv-hardware/src/federation_migration.rs`.
